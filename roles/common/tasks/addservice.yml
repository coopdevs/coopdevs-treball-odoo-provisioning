---
- name: Copy systemd service
  template:
    src="odoo.service.j2"
    dest="/etc/systemd/system/odoo.service"
    mode=2775
    owner="{{ default_group }}"
    group="{{ default_group }}"
  become: yes

- name: Enable service
  become: yes
  systemd:
    name: odoo.service
    enabled: yes
    state: started
    daemon_reload: yes
